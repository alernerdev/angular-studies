/* references to the vendor libraries.
This is used by webpack in the production build only
This gets built into a separate bundle
*/

/* eslint-disable no-unused-vars */
import angular from 'angular';