// ES6 syntax.  Transpiling needs to work in the
// build process to convert this to ES5
import chalk from 'chalk';

// ES5 syntax
//var chalk = require('chalk');

console.log(chalk.green('Starting app in dev mode...'));